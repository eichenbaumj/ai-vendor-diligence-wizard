# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Local env / secrets — NEVER commit credentials (public repo)
.env
.env.*
!.env.example
supabase/.env.local
supabase/.temp/
.dev.vars

# Editor / OS
.DS_Store
.vscode/
.idea/

# Test artifacts
coverage/
tests/redteam/results/

# local QA run output (when QA_PANEL_DIR is unset)
qa-runs/

# Private QA panels and session docs — real-vendor expectations and
# operational detail NEVER enter the public repo (docs/testing.md explains
# why). A unit test fails if anything under private/ becomes tracked.
private/
